const mongoose = require('mongoose');

// This will take the new student data such as courses completed, major choice, etc.
const newStudentSchema = mongoose.Schema({
    
});

module.exports = mongoose.model('User', userSchema);